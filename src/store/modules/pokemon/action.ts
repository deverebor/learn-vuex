import { ActionTree } from "vuex";
import * as types from "./types";

const actions: ActionTree<types.IPokeActions, types.IRoot> = {
  //TODO: criar uma action para a conexão com a API.
};

export default actions;
